.vscode/**
src/**
out/**
node_modules/**
.gitignore
.env
esbuild.js
tsconfig*.json
eslint.config.mjs
DRYRUN_SPEC.md
testbed/
ui-shots/
media/__ui-*.html
MANUAL-CHECK.md
examples/
*.tmp.mjs
*.tmp.ts

# Source maps. The production build does not emit them, but `vsce package` run
# on its own packages whatever is in dist/ — and a dev build there would ship
# the whole original TypeScript inside the extension.
dist/**/*.map

# Copy to .env and point at a database you are willing to have real statements
# executed against — staging, or a production replica. Not production itself:
# Dry Run refuses those by design.
#
# Every statement runs inside a transaction that is always rolled back, so
# nothing persists, but the work is real and so are the locks.
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/your_db
